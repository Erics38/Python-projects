# Copy this file to .env and fill in your actual values
# This file is safe to commit - it shows the structure but no real secrets

# AWS Configuration (required for Parameter Store access)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_HERE
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_KEY_HERE

# Setup Instructions:
# 1. Copy this file to .env
# 2. Replace YOUR_ACCESS_KEY_HERE and YOUR_SECRET_KEY_HERE with your AWS credentials
# 3. Run setup-parameters.sh to create all parameters in AWS Parameter Store
# 4. All database and email configuration is now stored in Parameter Store

# Note: The application now uses AWS Systems Manager Parameter Store for all sensitive configuration.
# No database passwords or email addresses need to be stored in environment variables!